import Foundation

/// Version marker for the KomgaKit package.
///
/// Placeholder public type introduced in Phase 1 to verify that the local
/// Swift package builds, links into the app target, and is testable.
public enum KomgaKitVersion {
    /// Current KomgaKit version string.
    public static let current = "0.1.0"
}
