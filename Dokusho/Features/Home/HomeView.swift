import SwiftUI

/// Home screen placeholder (Keep Reading / On Deck). Filled in Phase 4.
struct HomeView: View {
    var body: some View {
        PlaceholderScreen(title: "ホーム", systemImage: "house")
    }
}

#Preview {
    HomeView()
}
